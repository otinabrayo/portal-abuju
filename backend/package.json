{
    "name": "grievance-portal-backend",
    "version": "1.0.0",
    "description": "Backend for Grievance Portal",
    "main": "server.js",
    "scripts": {
        "start": "node start-prod.js",
        "dev": "nodemon start-dev.js",
        "init-db": "node -e \"require('dotenv').config(); require('./config/db').query(require('fs').readFileSync('./db/schema.sql', 'utf8'))\""
    },
    "dependencies": {
        "bcryptjs": "^2.4.3",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "pg": "^8.11.3"
    },
    "devDependencies": {
        "nodemon": "^3.0.2"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}